package optimizers
version v0r1p1

author James Chiang <jchiang@slac.stanford.edu>
author Pat Nolan <pln@razzle.stanford.edu>

# $Id$

use xml
use cfitsio v1* IExternal
use f2c v*
use CLHEP v2* IExternal
use python v2* IExternal

apply_pattern package_include
apply_pattern package_linkopts

# Kluge. Python isn't a package yet, so this is machine-dependent
macro pydir ../python/
#macro PYINCLUDE /usr/include/python2.2
#macro PYLIB /usr/lib/python2.2

#macro SWIG swig
#macro swig_nowarn "" Linux "-w"
#include_dirs $(PYINCLUDE)
#language swig -suffix=i -linker=$(clink)
#make_fragment swig 
#make_fragment swig_library

private

# allow use of internal path
macro optimizers_cppflags "" Linux "-I ../src -g -pedantic " WIN32 "/I..\src  /wd4290"
macro_prepend cflags "" Linux "-g"

# make sure test application gets built when library changes
macro test_optimizersstamps "optimizers.stamp"

macro my_headers "" WIN32 "../optimizers/*.h *.h"
library optimizers -no_share $(my_headers) *.f *.c *.cxx $(pydir)*.i

# test application
application test_optimizers test/*.cxx
